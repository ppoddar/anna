bew info postgresql

brew restart postgresql

DATA_DIR=/usr/local/var/postgres/
EXECUTABLE=/usr/local/bin/postgres 

    pg_ctl -D /usr/local/var/postgres/ -l logfile start

Login as super user
---------------------
sudo psql -U ppoddar postgres

create database anna;
alter user postgres with encrypted password 'passw0rd';
 grant all privileges on database anna to postgres;

 alter database anna owner to postgres;
 alter user postgres SUPERUSER; -- nedd to define pgcypto extensions